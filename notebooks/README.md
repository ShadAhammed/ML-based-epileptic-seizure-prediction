# Notebooks (local only)

The legacy research notebook (`legacy/Epilepsy.ipynb`) is **not tracked in git** (see `.gitignore`).

Keep your copy locally for model training. The public application uses the **dashboard** and expects a pre-trained model at `models/seizure_model.joblib`.

If you need the original notebook, retrieve it from an earlier commit or the upstream research archive.
